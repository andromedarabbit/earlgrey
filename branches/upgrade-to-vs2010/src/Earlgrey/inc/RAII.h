#pragma once

#include <type_traits>

namespace Earlgrey
{
	//////////////////////////////////////////////////////////////////////////
	// ARRAY
	//////////////////////////////////////////////////////////////////////////
	// TODO: 다른 Deleter 는 다른 곳에 있다. 정리할까?
	template <typename T>
	class ArrayDeleter 
	{
	public:
		void operator ()( T const * p)
		{ 
			delete[] p; 
		}
	};

	//////////////////////////////////////////////////////////////////////////
	// HANDLE
	//////////////////////////////////////////////////////////////////////////
	typedef std::tr1::shared_ptr< std::tr1::remove_pointer<HANDLE>::type > handle_t;
}