#include "stdafx.h"
#include "FileVersionInfo.h"
#include "EarlgreyProcess.h"
#include "txstring.h"

namespace Earlgrey
{
	namespace Test
	{
		TEST(FileVersionInfoTest, PushAndPop)
		{
		/*
			_txstring fileName( Process::MainModuleFileName() );

			_txstring versionInfo( FileVersionInfo::GetVersionInfo(fileName) );
			_txstring fileVersion = versionInfo.FileVersion();
			DBG_UNREFERENCED_LOCAL_VARIABLE(fileVersion);
			*/
		}
	}
}